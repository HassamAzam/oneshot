node_modules/
state/
# DRY_RUN gives a run its own home so a refused write cannot corrupt the real
# state/ — src/lib/config.ts. `npm run score` sets it, so this appears locally.
state-dry/
logs/
.env
.env.*
!.env.example
*.log
.DS_Store
/.claude/

# Scratch files phases drop at the conductor root (Bash redirects escape write-scope)
/qa5-*.json
/qa5-*.cjs
/*-auth.json
/*-recon.cjs
/otel-*.mts

# document phase stages attachments here before upload_markdown (it rejects absolute paths)
/evidence_upload/

# Planning docs never get committed
/docs/superpowers/
