{
  "name": "oneshot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "One orchestrator, one label, one ticket at a time: a deterministic conductor that drives a GitLab ticket from label to Ready For Deployment through fresh per-phase Claude sessions.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "tsx src/index.ts",
    "watch": "tsx src/index.ts --watch-only",
    "doctor": "tsx scripts/doctor.ts",
    "hooks:install": "bash scripts/install-hooks.sh",
    "hooks:uninstall": "bash scripts/uninstall-hooks.sh",
    "hooks:verify": "bash scripts/verify-hooks.sh",
    "check": "tsc --noEmit && for f in hooks/*.js; do node --check \"$f\" || exit 1; done && echo 'check clean'"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.1.77",
    "better-sqlite3": "^12.11.1",
    "dotenv": "^16.6.1",
    "tsx": "^4.23.11"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^22.20.1",
    "typescript": "^5.9.3"
  }
}
